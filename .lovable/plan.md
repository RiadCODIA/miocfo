

## Add "Analisi AI in tempo reale" to "Tutti i piani includono"

Simple text addition in two files where the `allInclude` array is defined:

1. **`src/pages/Landing.tsx` line 87** — Add `"Analisi AI in tempo reale"` to the array
2. **`src/pages/PianiPricing.tsx` lines 92-97** — Add `"Analisi AI in tempo reale"` to the array

Both arrays get the same new entry appended.

